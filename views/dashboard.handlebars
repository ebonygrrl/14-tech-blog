                    <div class="text-center mb-3">
                        <a href="/dashboard/post/new"><button class="btn btn-primary btn-lg btn-">+ New Post</button></a>
                    </div>

                    {{#each posts as |post|}}
                    {{> db-posts}}
                    {{/each}}