                    {{#if loggedIn}}
                    {{#each posts as |post|}}
                    {{> posts loggedIn=true}}
                    {{/each}}
                    {{else}}
                    {{#each posts as |post|}}
                    {{> posts}}
                    {{/each}}
                    {{/if}}