                    <div class="card mb-3">
                        <div class="card-header">
                            &nbsp;
                        </div>
                        <div class="card-body">
                            <p class="card-text">{{comment}}</p>
                            <p>&ndash; {{user.username}}, {{format_date created_at}}</p>
                        </div>
                    </div>