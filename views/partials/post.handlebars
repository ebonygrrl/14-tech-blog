                    <!-- / | View All Posts -->
                    <div class="card mb-3">
                        <div class="card-header">
                            <h2>{{title}} <span>Posted by {{user.username}} on {{created_at}}</span></h2>
                        </div>
                        <div class="card-body">
                            <h5 class="card-title">{{title}}</h5>
                            <p class="card-text">{{content}}
                            </p>
                        </div>
                    </div>